# Archived input, not the active implementation

The surrounding files reproduce the user-supplied original archive. The old
multicomponent script double-weights mass fractions and should not be used for
new results. The notebook includes exploratory formulas and stored plots that
are not validated by the new engine. Use `../launch.py` and
`../xray_workbench/physics.py` for the corrected implementation.
